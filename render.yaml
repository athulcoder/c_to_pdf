services:
  - type: web
    name: flask-socketio-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -u main.py
    envVars:
      - key: PORT
        value: 8000
